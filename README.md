# maxelee.github.io

Personal academic website of Max E. Lee — cosmology PhD candidate at Columbia University studying baryonic effects on weak gravitational lensing.

Built with Jekyll, served by GitHub Pages (classic build from `master`).

Local development:

```sh
bundle install --path vendor/bundle
JEKYLL_ENV=production bundle exec jekyll build   # or `jekyll serve`
```
